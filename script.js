const slider=document.getElementById('compareSlider');const layer=document.getElementById('shaderLayer');const divider=document.getElementById('divider');function update(){const v=slider.value;layer.style.width=v+'%';divider.style.left=v+'%'}slider.addEventListener('input',update);update();